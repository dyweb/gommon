package main

import (
	"fmt"

	// TODO: it seems you can use package with _ prefix
	"github.com/dyweb/gommon/log2/_examples/uselib/foo"
)

// TODO: make it a full example, like user auth with multiple mock backends
func main() {
	fmt.Println(foo.FOO)
}
